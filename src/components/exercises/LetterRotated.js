import React from 'react';
import { View } from 'react-native';

import { RoundTextButton } from '../Components';

const LettersRotated = ({ letters, rotated }) => {
return (
    <View>
      <RoundTextButton/>
    </View>
  );
};

export default LettersRotated;