import React from 'react';
import { storiesOf } from '@kadira/react-native-storybook';
import LetterSketchExample from './LetterSketchExample';

storiesOf('Letter Sketch', module).add('Example', () => (
  <LetterSketchExample />
));
